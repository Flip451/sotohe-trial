[package]
name = "web"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[[bin]]
name = "app"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
interface_adapters = { path = "../../libs/interface_adapters" }
web_composition = { path = "../web-composition" }
tokio = { version = "1", features = ["macros", "rt"] }
